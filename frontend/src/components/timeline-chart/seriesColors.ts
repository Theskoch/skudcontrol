// Fixed identity colors for the timeline chart's two data sources — not tied
// to attendance-quality status (that's shown separately via the hero score).
// Both are validated categorical hues from the dashboard's dark palette.
export const SKUD_HEX = "#9085e9"; // violet — matches the app's brand accent
export const WIFI_HEX = "#3987e5"; // blue
